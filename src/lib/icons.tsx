/**
 * Icon conventions for OpenRecon.
 * All icons come from lucide-react.
 * Import from here rather than directly from lucide-react
 * so conventions stay consistent across the app.
 */

export {
  // Instruments
  ScanSearch   as IconSherlock,
  Globe2       as IconRecon,
  ImageIcon    as IconExif,
  SearchCode   as IconDorks,
  FolderSearch as IconInvestigation,

  // Actions
  Search       as IconSearch,
  Copy         as IconCopy,
  Check        as IconCheck,
  ExternalLink as IconExternal,
  RotateCcw    as IconRetry,
  ChevronDown  as IconChevronDown,
  ChevronRight as IconChevronRight,
  X            as IconClose,
  Menu         as IconMenu,
  ArrowRight   as IconArrow,
  Plus         as IconPlus,
  Play         as IconPlay,
  Link2        as IconLink,
  FileText     as IconReport,
  FileText     as IconFile,
  FolderOpen   as IconFolderOpen,

  // Data / results
  MapPin       as IconLocation,
  Network      as IconNetwork,
  Server       as IconDns,
  Smartphone   as IconDevice,
  Camera       as IconCamera,
  Clock        as IconTime,
  Shield       as IconSecurity,
  AlertCircle  as IconError,
  Info         as IconInfo,

  // States
  Loader2      as IconLoading,
} from "lucide-react";
